package com.company.tenbitsAssignment5;

import java.util.Arrays;

public class SortNames {
    public static void sortFromArray(String[] names){
        Arrays.sort(names);
        System.out.println("Array after sorting names "+ Arrays.toString(names));
    }
}
